export const metadata = {
    title: "About Us | कारोbaar",
    description: "Learn about the mission and vision behind कारोbaar, built by developers for the wholesale industry.",
};

export default function AboutLayout({ children }) {
    return <>{children}</>;
}
